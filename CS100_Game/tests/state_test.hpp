#ifndef STATE_TEST_HPP
#define STATE_TEST_HPP

#include "../src/states/State.h"
#include "gtest/gtest.h"

TEST(StateTest, Default){
    EXPECT_EQ(0, 0);
    EXPECT_EQ(0, 0);
}

#endif // STATE_TEST_HPP
